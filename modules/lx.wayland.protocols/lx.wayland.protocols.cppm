module;

export module lx.wayland.protocols;

export import lx.wayland.protocols.lumen;

// Upstream protocol modules (lx.wayland.protocols.core, .xdg, .dmabuf, …)
// are generated from protocols/manifest.toml and imported directly by
// lx.wayland.server / lx.wayland.client as needed.
